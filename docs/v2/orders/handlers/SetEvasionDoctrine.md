# SET_EVASION_DOCTRINE

Sets the unit's tactical response to incoming threats.

## Parameters

| Parameter | Type | Description |
| :--- | :--- | :--- |
| `doctrine` | `string` | The evasion policy (e.g., `OFF`, `AUTOMATIC`, `AGGRESSIVE`). |

## Effect

*(Currently a Placeholder)*

1.  **Future Implementation**: Will configure the `DoctrineSystem` to trigger specific maneuvers (e.g., "beam" the threat) or countermeasure deployments when a threat is detected.
