# SET_DC_PRIORITY

Sets the priority for Damage Control (DC) teams.

## Parameters

| Parameter | Type | Description |
| :--- | :--- | :--- |
| `priority` | `string` | The component type to prioritize (e.g., `ENGINES`, `SENSORS`). |

## Effect

*(Currently a Placeholder)*

1.  **Future Implementation**: Will modify the repair rate coefficients in the `HealthSystem` for specific component slots.
